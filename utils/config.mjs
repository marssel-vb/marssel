export const FontsConfig = {
    fontsPath: "/public/fonts", // Chemin pour le web (avec slash initial)
    manifestPath: "/js/fonts-manifest.json", // Chemin pour le web (avec slash initial)
};

export const IconsConfig = {
    fontsPath: "/public/images/icons", // Dossier contenant les icones SVG personnalisées
    manifestPath: "/js/icons-manifest.json", // Fichier de sortie du manifeste
    outputIcons: "/js/icons.json", // Fichier de sortie pour les icones JavaScript
};
